version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: qunarly-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: qunarly
    ports:
      - "5432:5432"
    volumes:
      - qunarly_pgdata:/var/lib/postgresql/data

volumes:
  qunarly_pgdata:
